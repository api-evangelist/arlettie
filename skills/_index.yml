generated: '2026-08-17'
method: searched
source: https://lesechappees.arlettie.com/agents.md
note: >-
  Arlettie publishes its own agent operating instructions. arlettie-agents.md is that document saved
  VERBATIM — it is not generated by API Evangelist. The store advertises it three ways: as /agents.md,
  mirrored at /llms.txt, named in the header comments of /robots.txt, and as the single entry in
  /sitemap_agentic_discovery.xml. The two files below are the provider's own text; no skills were
  authored on Arlettie's behalf, because there is no OpenAPI to ground authored steps in.

skills:
- file: arlettie-agents.md
  name: Agent Instructions — Arlettie Les échappées
  provider_published: true
  method: searched
  source: https://lesechappees.arlettie.com/agents.md
  http_status: 200
  content_type: text/markdown
  api: mcp/arlettie-mcp.yml
  covers:
  - UCP discovery via /.well-known/ucp
  - the six-step agent flow (discover, search, cart, checkout, fulfill, complete)
  - the human-approval invariant on payment
  - rate-limit backoff on 429
  - buyer context (address_country, currency) for pricing and availability
  - read-only browsing endpoints that need no authentication
  operations: [search_catalog, create_cart, create_checkout, update_checkout, complete_checkout]
  operations_note: >-
    Tool names as written in the provider's own document. Note that agents.md names create_cart and
    create_checkout, which exist on the current /api/ucp/mcp endpoint — the document describes the UCP
    endpoint, not the deprecated /api/mcp one.

- file: ../llms/arlettie-llms.txt
  name: llms.txt (mirror of agents.md)
  provider_published: true
  method: searched
  source: https://lesechappees.arlettie.com/llms.txt
  http_status: 200
  content_type: text/markdown
  note: >-
    The provider states "You're reading /llms.txt, which mirrors that content" — agents.md is the
    canonical document. Retained separately because the LLMsTxt pointer is a distinct discovery surface.

third_party_referenced:
- name: Shop skill
  url: https://shop.app/SKILL.md
  publisher: Shopify
  note: >-
    Arlettie's own instructions recommend this skill for buy-for-me agents in preference to scripting
    the storefront. It is Shopify's document, not Arlettie's, so it is referenced here and NOT vendored
    into this repo.

generated_skills: []
generated_skills_note: >-
  None. Authoring skills would require real operationIds from an OpenAPI, and Arlettie publishes none.
  The 13 MCP tools in mcp/arlettie-mcp.yml are the callable surface, and the provider's own agents.md
  already documents the flow across them better than a derived skill could.
